// export const cartProducts = state => {
// 	return state.cart.added.map(({ style, size ,quantity}) => {
// 		const product = state.product.all.find(d => {
// 			return d.style === style && d.size === size
// 		})
// 		return {
// 			name: products.name,
// 			style: products.style,
// 			size: products.size,
// 			price: products.price,
// 			quantity
// 		}
// 	})
// }